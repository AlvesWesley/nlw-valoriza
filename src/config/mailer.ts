import { mailerHost, mailerPort, mailerUser, mailerPass } from '../utils/env'

const config = {
  host: mailerHost,
  port: mailerPort,
  user: mailerUser,
  pass: mailerPass
}

export default config
